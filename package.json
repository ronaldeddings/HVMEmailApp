{
  "name": "hvm-email-app",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "setup-watch": "bun run scripts/setup-watch.ts",
    "renew-watches": "bun run scripts/renew-watches.ts",
    "list-users": "bun run scripts/list-users.ts"
  },
  "dependencies": {
    "@google-cloud/pubsub": "^5.3.0",
    "google-auth-library": "^9.0.0",
    "googleapis": "^144.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
